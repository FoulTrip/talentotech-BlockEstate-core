export class PropertyController {
    AddProperty(req, res) {
        try {
            const {
                title,
                description,
                address,
                city,
                country,
                latitude,
                longitude,
                price,
                estimatedValue,
                tokenSymbol,
                totalTokens,
            } = req.body;

            
        } catch (error) {

        }
    }
}